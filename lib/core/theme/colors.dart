import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF004AAD);     // Deep blue (logo base)
  static const Color accent = Color(0xFF8DC63F);      // Green (used for highlights)
  static const Color background = Color(0xFFF6F6F6);  // Light grayish background
  static const Color surface = Color(0xFFFFFFFF);     // Cards, forms
  static const Color textPrimary = Color(0xFF1A1A1A); // Dark gray for main text
  static const Color textSecondary = Color(0xFF6B6B6B); // Subtext
  static const Color error = Colors.redAccent;
}
