import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF004AAD);     // Deep blue (logo base)
  static const Color accent = Color(0xFF8DC63F);      // Green (used for highlights)
  static const Color background = Color(0xFFFEF9EC);  // Soft yellow
  static const Color surface = Color(0xFFFFFFFF);     // Cards, forms
  static const Color textPrimary = Color(0xFF648A75);   // ✅ New darker green
  static const Color textSecondary = Color(0xFF8A8A8A); // Optional adjustment
  static const Color error = Colors.redAccent;
}
